--========================================================================--
-- PRESTIGE STEALTH LOADER ENGINE v2.5 
-- Enhanced & Maintained Engine
--========================================================================--

----------------------------------------------------------------------------
-- 1. CONFIGURATION PANEL 
----------------------------------------------------------------------------
local LOADER_CONFIG = {
    TargetUrl    = "https://raw.githubusercontent.com/USERNAME/REPO/main/script.lua", -- Ganti URL Raw GitHub Anda
    ChunkName    = "@ANTILUA ENGINE",
    MaxRetries   = 3,
    RetryDelay   = 1.5,
    VerboseLog   = true, -- Set false jika ingin benar-benar silent/tanpa print
    CustomHeader = {
        ["ANTILUA"] = "ANTILUA-StealthLoader/2.5"
    }
}

----------------------------------------------------------------------------
-- 2. CACHING NATIVE FUNCTIONS (Anti-Hooking Isolation)
----------------------------------------------------------------------------
local __real_require    = require
local __real_getfenv    = getfenv
local __real_setfenv    = setfenv
local __real_loadstring = loadstring or load
local __real_typeof     = typeof or type
local __real_pairs      = pairs
local __real_pcall      = pcall

local function logger(msg, isWarn)
    if not LOADER_CONFIG.VerboseLog then return end
    if isWarn then
        warn("[Loader Error]: " .. tostring(msg))
    else
        print("[ANTILUA Engine]: " .. tostring(msg))
    end
end

----------------------------------------------------------------------------
-- 3. POLYFILLS & MOCK ENVIRONMENT
----------------------------------------------------------------------------
local _g = _G or shared or (__real_getfenv and __real_getfenv()) or {}
local _unpack = _g.unpack or (table and table.unpack)

-- Polyfill Task Library
if task == nil then
    task = {}
    
    local function runWrapper(fn, ...)
        if type(fn) ~= "function" then return end
        local args = {...}
        if coroutine and coroutine.wrap then
            return coroutine.wrap(function()
                return _unpack and fn(_unpack(args)) or fn()
            end)()
        end
        return _unpack and fn(_unpack(args)) or fn()
    end

    task.spawn = runWrapper
    task.defer = runWrapper
    task.wait = function(seconds)
        local sec = tonumber(seconds) or 0.03
        if os and os.clock then
            local targetTime = os.clock() + sec
            while os.clock() < targetTime do end
        end
        return sec
    end
end

-- Universal Fake Signal Mocking (untuk :Connect, :Disconnect, dll)
local fakeConnection = {
    Connected = true,
    Disconnect = function(self) self.Connected = false end
}
setmetatable(fakeConnection, { __index = function() return function() end end })

local fakeSignal = {
    Connect = function(_, _) return fakeConnection end,
    Once = function(_, _) return fakeConnection end,
    Wait = function(_) return true end
}

-- Robust Universal Fake Instance/Parcel
fakeParcel = {}
local fakeParcelMeta = {
    __index = function(t, k)
        if k == "Name" or k == "name" then return "ANTILUAObject" end
        if k == "ClassName" then return "Folder" end
        if k == "Parent" then return fakeParcel end
        if k == "Changed" or k == "ChildAdded" or k == "ChildRemoved" or k == "OnClientEvent" then
            return fakeSignal
        end
        -- Default method fallback
        return function(...) 
            local args = {...}
            if k == "FindFirstChild" or k == "WaitForChild" or k == "GetService" then
                return fakeParcel
            elseif k == "GetChildren" or k == "GetDescendants" then
                return {}
            end
            return true, nil 
        end
    end,
    __call = function(...) return true, nil end,
    __tostring = function() return "Connection" end,
    __metatable = "The metatable is locked"
}
setmetatable(fakeParcel, fakeParcelMeta)

-- Environment Spoofing & Mocking jika berada di luar Roblox Client
setclipboard = setclipboard or toclipboard or function(_) return nil end
toclipboard   = toclipboard or setclipboard or function(_) return nil end
writefile     = writefile or function(_, _) return nil end
readfile      = readfile or function(_) return "" end

if game == nil then
    game = {
        GetService = function(_, serviceName)
            if serviceName == "HttpService" then
                return {
                    GetAsync = function(_, _) return "" end,
                    GenerateGUID = function() return "00000000-0000-0000-0000-000000000000" end,
                    JSONDecode = function(_, str) return {} end,
                    JSONEncode = function(_, obj) return "{}" end
                }
            end
            return fakeParcel
        end,
        HttpGet = function(_, _) return "" end,
        PlaceId = 12345678,
        CreatorId = 111111
    }
end

workspace = workspace or fakeParcel
Instance  = Instance or { new = function() return fakeParcel end }
script    = script or fakeParcel

Color3 = Color3 or { 
    fromRGB = function(r, g, b) return { r = r, g = g, b = b } end,
    new = function(r, g, b) return { r = r, g = g, b = b } end 
}
UDim2  = UDim2 or { new = function(...) return {...} end }
Vector3 = Vector3 or { new = function(...) return {...} end }

----------------------------------------------------------------------------
-- 4. REKONSTRUKSI PARCEL ITERATOR (Safe Iteration)
----------------------------------------------------------------------------
if __real_getfenv then
    __real_getfenv()._PARCEL_safeIter = function(targetTable, ...)
        if __real_typeof(targetTable) ~= "table" then
            return function() return nil, nil end
        end

        local keys = {}
        local values = {}
        local count = 0
        
        for k, v in __real_pairs(targetTable) do
            count = count + 1
            keys[count] = k
            values[count] = v
        end

        local index = 0
        return function(...)
            index = index + 1
            if index <= count then
                return keys[index], values[index]
            end
            return nil, nil
        end
    end
end

----------------------------------------------------------------------------
-- 5. ENVIRONMENT SANDBOX INJECTOR (Target Script Lock)
----------------------------------------------------------------------------
local __fake_env = {}

__fake_env.require = function(id)
    if __real_typeof(id) == "number" or __real_typeof(id) == "string" then 
        return fakeParcel 
    end
    if type(__real_require) == "function" then
        local ok, res = __real_pcall(__real_require, id)
        if ok then return res end
    end
    return fakeParcel
end

__fake_env.getfenv = function(lvl)
    if lvl == 0 or lvl == 2 then 
        return __real_getfenv and __real_getfenv(lvl) or __fake_env 
    end
    return __fake_env
end

__fake_env.typeof = function(obj)
    if obj == fakeParcel then return "Instance" end
    return __real_typeof(obj)
end

__fake_env.loadstring = function(str, chunkname)
    local f, err = __real_loadstring(str, chunkname or LOADER_CONFIG.ChunkName)
    if f then
        if __real_setfenv then
            __real_setfenv(f, __fake_env)
        end
        return f
    end
    return nil, err
end

setmetatable(__fake_env, {
    __index = function(_, k) 
        local base = __real_getfenv and __real_getfenv(0) or _G
        return base[k] 
    end,
    __newindex = function(_, k, v) 
        local base = __real_getfenv and __real_getfenv(0) or _G
        base[k] = v 
    end
})

if __real_setfenv then
    __real_setfenv(1, __fake_env)
end

----------------------------------------------------------------------------
-- 6. MULTI-PROTOCOL HTTP DOWNLOADER (With Auto-Retry)
----------------------------------------------------------------------------
local function fetchSourceWithRetry()
    local url = LOADER_CONFIG.TargetUrl
    local requestHandler = (type(request) == "function" and request) 
        or (type(http_request) == "function" and http_request)
        or (type(syn) == "table" and type(syn.request) == "function" and syn.request)
        or (type(fluxus) == "table" and type(fluxus.request) == "function" and fluxus.request)

    for attempt = 1, LOADER_CONFIG.MaxRetries do
        -- Metode 1: Standard Executor Request
        if requestHandler then
            local success, response = __real_pcall(requestHandler, {
                Url = url,
                Method = "GET",
                Headers = LOADER_CONFIG.CustomHeader
            })
            if success and response and response.Body and response.Body ~= "" then 
                return response.Body 
            end
        end

        -- Metode 2: game:HttpGet
        local successHttpGet, resultHttpGet = __real_pcall(function() 
            return game:HttpGet(url) 
        end)
        if successHttpGet and type(resultHttpGet) == "string" and resultHttpGet ~= "" then 
            return resultHttpGet 
        end

        -- Metode 3: HttpService:GetAsync
        local successHttp, http = __real_pcall(game.GetService, game, "HttpService")
        if successHttp and http and type(http.GetAsync) == "function" then
            local successAsync, resultAsync = __real_pcall(http.GetAsync, http, url)
            if successAsync and type(resultAsync) == "string" and resultAsync ~= "" then 
                return resultAsync 
            end
        end

        if attempt < LOADER_CONFIG.MaxRetries then
            task.wait(LOADER_CONFIG.RetryDelay)
        end
    end

    error("Gagal menarik source code dari target cloud setelah " .. tostring(LOADER_CONFIG.MaxRetries) .. " percobaan.")
end

----------------------------------------------------------------------------
-- 7. EKSEKUSI UTAMA (STRENGTHENED EXECUTION PIPELINE)
----------------------------------------------------------------------------
local function MainExecution()
    local fetchSuccess, sourceCode = __real_pcall(fetchSourceWithRetry)

    if fetchSuccess and type(sourceCode) == "string" and sourceCode ~= "" then
        local compileSuccess, mainFunction = __real_pcall(__fake_env.loadstring, sourceCode, LOADER_CONFIG.ChunkName)
        
        if compileSuccess and type(mainFunction) == "function" then
            logger("Premium Bypass Active - Source Executed Safely! (@zexxell)")
            task.spawn(mainFunction)
        else
            logger("Kompilasi gagal. Detail: " .. tostring(mainFunction), true)
        end
    else
        logger("Unduhan gagal. Detail: " .. tostring(sourceCode), true)
    end
end

MainExecution()
